name: Deploy to production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Copy files to VM
        run: |
          rsync -avz --delete -e "ssh -o StrictHostKeyChecking=no" ./ ${{ secrets.VM_USER }}@${{ secrets.VM_HOST }}:~/Anomologita

      - name: Deploy with Docker Compose
        env:
          SA_PASSWORD: ${{ secrets.SA_PASSWORD }}
          JWT_KEY: ${{ secrets.JWT_KEY }}
          AZURE_BLOB_CONNECTION_STRING: ${{ secrets.AZURE_BLOB_CONNECTION_STRING }}
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.VM_USER }}@${{ secrets.VM_HOST }} "
            cd ~/Anomologita &&
            export SA_PASSWORD='${SA_PASSWORD}' &&
            export JWT_KEY='${JWT_KEY}' &&
            export AZURE_BLOB_CONNECTION_STRING='${AZURE_BLOB_CONNECTION_STRING}' &&
            docker compose pull &&
            docker compose up -d --build
          "

      - name: Run EF Core migrations as one-off container
        env:
          SA_PASSWORD: ${{ secrets.SA_PASSWORD }}
          JWT_KEY: ${{ secrets.JWT_KEY }}
          AZURE_BLOB_CONNECTION_STRING: ${{ secrets.AZURE_BLOB_CONNECTION_STRING }}
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.VM_USER }}@${{ secrets.VM_HOST }} "
            cd ~/Anomologita &&
            SA_PASSWORD='${SA_PASSWORD}' JWT_KEY='${JWT_KEY}' AZURE_BLOB_CONNECTION_STRING='${AZURE_BLOB_CONNECTION_STRING}' \
            docker compose run --rm anomologita-api dotnet ef database update --no-build
          "
